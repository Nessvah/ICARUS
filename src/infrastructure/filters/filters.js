// If database type is 'mysql', then adapt filter operators logic to sql (knext).

// apply logical operators to filter values. - where + _and / _or

//apply value operator to filter values. - where + _eq / _neq / _lt / _lte / _gt / _gte / _in / _nin
